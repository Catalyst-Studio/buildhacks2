import type { API } from '@onehop/js';
export declare type Connection = {
    edge_endpoint: string;
};
export declare type Payload = {
    pipe_room: API.Pipe.Room;
    connection: {
        llhls?: Connection;
        webrtc?: Connection;
    };
};
export declare const PIPE_ROOM_AVAILABLE: import("./create").LeapHandler;
