import type { API } from '@onehop/js';
export declare type Data = {
    channels: API.Channels.Channel[];
    metadata: API.Channels.State;
    cid: string;
    connection_count: number;
    scope: 'project' | 'token';
};
export declare const INIT: import("./create").LeapHandler;
