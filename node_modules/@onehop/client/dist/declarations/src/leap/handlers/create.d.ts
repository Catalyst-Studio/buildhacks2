import type { API } from '@onehop/js';
import type { LeapServiceEvent } from '@onehop/leap-edge-js';
import type { Client } from '..';
export declare type LeapHandler = {
    handle(client: Client, event: LeapServiceEvent): Promise<void>;
};
export declare function createLeapEvent<D, G extends boolean = true>(config: {
    requireId?: G;
    handle: (client: Client, channelId: G extends true ? API.Channels.Channel['id'] : API.Channels.Channel['id'] | null, data: D) => Promise<void>;
}): LeapHandler;
