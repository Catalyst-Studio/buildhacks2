import type { HlsConfig } from 'hls.js';
import Hls from 'hls.js';
export declare const APPLE_HLS_MIME = "application/vnd.apple.mpegurl";
export declare const LIVE_LLHLS_SYNC_BCE = 3;
export declare const WCL_DELAY_LES = 5;
export declare class Controls {
    private readonly node;
    private readonly _hls;
    constructor(node: HTMLVideoElement, hls?: Hls);
    /**
     * Syncs to live edge
     *
     * @param distance The seconds to sync from live edge (e.g. a buffer)
     */
    sync(distance?: number): void;
    stop(): Promise<void>;
    play(): Promise<void>;
    get isPaused(): boolean;
    destroy(): void;
    get isNative(): boolean;
    get hls(): Hls;
}
export declare function mount(node: HTMLVideoElement, url: string, hlsConfigOverride?: Partial<HlsConfig>): Controls;
